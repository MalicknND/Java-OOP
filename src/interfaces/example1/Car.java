package interfaces.example1;

/*Interface is a blueprint for a class.
It defines a set of methods that a class must implement, but it does not provide any implementation.
An interface acts as a contract between the class and the outside world.
It defines what the class is capable of doing, but not how it does it.*/

public interface Car {

    void startEngine();

    void accelerate();

    void brake();
}